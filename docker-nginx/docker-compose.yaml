services: 

  app:
    build: 
      context: node
    container_name: app
    networks: 
      - node-network
    volumes: 
      - ./node:/usr/src/app
    tty: true
    ports: 
      - "8080:3000"
    depends_on: 
       - db

  db:
    build: 
      context: mysql
    command: --innodb-use-native-aio=0
    container_name: db
    restart: always
    tty: true
    volumes: 
      - ./mysql_data:/var/lib/mysql
    networks: 
      - node-network

networks: 
  node-network:
    driver: bridge      
